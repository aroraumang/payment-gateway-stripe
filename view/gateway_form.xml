<?xml version="1.0"?>
<data>
    <xpath expr="/form/notebook" position="inside">
        <page string="Stripe Settings" id="stripe"
                states="{'invisible': Eval('provider') != 'stripe'}">
            <label name="stripe_api_key_test" />
            <field name="stripe_api_key_test" widget="password" />
            <label name="stripe_api_key_live" />
            <field name="stripe_api_key_live" widget="password" />
        </page>
    </xpath>
</data>
